class Marker {
  constructor(location, options) {
    this.location = location;
    this.options = options;
    // this.marker = L.marker(this.location.latLng, this.options);
  }
}

export default Marker;
